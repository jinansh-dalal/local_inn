# local_inn

```
python -m venv local-inn
source local-inn/bin/activate
pip install torch
pip install numpy
pip install FrEIA
pip install tqdm
pip install tensorboard
pip install PyYAML
pip install rosbags
```
